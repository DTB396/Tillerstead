/**
 * auto-contrast.js
 * Legacy stub retained to avoid 404s; real contrast logic was removed with theme refactor.
 */
(() => {
  'use strict';

  if (typeof window !== 'undefined') {
    window.autoContrast = () => {};
  }
})();
